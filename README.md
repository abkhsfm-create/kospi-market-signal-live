# KOSPI Market Signal — live public snapshot

This repository contains only the sanitized JSON used by the public research dashboard.
The forecasting model, full history, source responses, and credentials remain in a separate
private repository. `dashboard.json` is generated automatically on KRX trading days.

The forecast is for research and reference only and is not investment advice.
